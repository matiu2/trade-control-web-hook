{
  "fires": [
    {
      "rule_id": "09-enter-qm",
      "action": "enter",
      "candle_time": "2026-08-27T13:00:00Z",
      "candle_close": 15.9798
    },
    {
      "rule_id": "09-enter-qm",
      "action": "enter",
      "candle_time": "2026-08-28T08:00:00Z",
      "candle_close": 15.9784
    },
    {
      "rule_id": "01-veto-too-high",
      "action": "veto",
      "candle_time": "2026-08-28T13:00:00Z",
      "candle_close": 16.0428
    }
  ],
  "done": true,
  "final_phase": "done",
  "warnings": [
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 305-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 305-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 305-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 305-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 306-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 306-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 306-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 306-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 307-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 307-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 307-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 307-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 308-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 308-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 308-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 308-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 309-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 309-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 309-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 309-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 310-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 310-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 310-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 310-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 311-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 311-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 311-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 311-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 312-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 312-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 312-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 312-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 313-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 313-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 313-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 313-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 314-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 314-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 314-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 314-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 315-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 315-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 315-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 315-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 316-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 316-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 316-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 316-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 317-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 317-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 317-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 317-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 318-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 318-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 318-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 318-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 319-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 319-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 319-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 319-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 320-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 320-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 320-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 320-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 321-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 321-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 321-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 321-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 322-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 322-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 322-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 322-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 323-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 323-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 323-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 323-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 324-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 324-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 324-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 324-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 325-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 325-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 325-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 325-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 326-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 326-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 326-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 326-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 327-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 327-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 327-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 327-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 328-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 328-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 328-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 328-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 329-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 329-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 329-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 329-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 330-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 330-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 330-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 330-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 331-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 331-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 331-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 331-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 332-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 332-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 332-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 332-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1785506400) is outside the 333-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1785780000) is outside the 333-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1785506400) is outside the 333-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1785780000) is outside the 333-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window."
  ],
  "outcome": {
    "net_r": -1.0,
    "tp_hits": 0,
    "sl_hits": 1,
    "reversal_closes": 0,
    "expiry_closes": 0,
    "invalidation_closes": 0,
    "open_at_end": 0,
    "legs": [
      {
        "entry_time": "2026-08-27T14:00:00Z",
        "entry_price": 15.9869,
        "stop_loss": 16.026900000000005,
        "take_profit": 15.8006,
        "exit_time": "2026-08-28T13:00:00Z",
        "exit_price": 16.026900000000005,
        "exit_reason": "stopped_out",
        "r": -1.0
      }
    ]
  }
}
