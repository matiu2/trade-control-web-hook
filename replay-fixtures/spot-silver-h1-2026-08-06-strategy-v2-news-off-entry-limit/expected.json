{
  "fires": [
    {
      "rule_id": "09-enter-qm",
      "action": "enter",
      "candle_time": "2026-08-06T14:00:00Z",
      "candle_close": 6167.9
    },
    {
      "rule_id": "01-veto-too-high",
      "action": "veto",
      "candle_time": "2026-08-07T03:00:00Z",
      "candle_close": 6237.0
    }
  ],
  "done": true,
  "final_phase": "done",
  "warnings": [
    "trendline 03-prep-break-and-close: anchor a (epoch 1779184800) is outside the 292-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1779364800) is outside the 292-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1779184800) is outside the 292-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1779364800) is outside the 292-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1779184800) is outside the 293-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1779364800) is outside the 293-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1779184800) is outside the 293-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1779364800) is outside the 293-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1779184800) is outside the 294-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1779364800) is outside the 294-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1779184800) is outside the 294-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1779364800) is outside the 294-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1779184800) is outside the 295-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1779364800) is outside the 295-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1779184800) is outside the 295-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1779364800) is outside the 295-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1779184800) is outside the 296-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1779364800) is outside the 296-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1779184800) is outside the 296-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1779364800) is outside the 296-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1779184800) is outside the 297-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1779364800) is outside the 297-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1779184800) is outside the 297-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1779364800) is outside the 297-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1779184800) is outside the 298-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1779364800) is outside the 298-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1779184800) is outside the 298-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1779364800) is outside the 298-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1779184800) is outside the 299-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1779364800) is outside the 299-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1779184800) is outside the 299-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1779364800) is outside the 299-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1779184800) is outside the 300-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1779364800) is outside the 300-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1779184800) is outside the 300-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1779364800) is outside the 300-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1779184800) is outside the 301-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1779364800) is outside the 301-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1779184800) is outside the 301-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1779364800) is outside the 301-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1779184800) is outside the 302-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1779364800) is outside the 302-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1779184800) is outside the 302-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1779364800) is outside the 302-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1779184800) is outside the 303-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1779364800) is outside the 303-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1779184800) is outside the 303-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1779364800) is outside the 303-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1779184800) is outside the 304-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1779364800) is outside the 304-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1779184800) is outside the 304-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1779364800) is outside the 304-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1779184800) is outside the 305-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1779364800) is outside the 305-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1779184800) is outside the 305-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1779364800) is outside the 305-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor a (epoch 1779184800) is outside the 306-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 03-prep-break-and-close: anchor b (epoch 1779364800) is outside the 306-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor a (epoch 1779184800) is outside the 306-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window.",
    "trendline 04-prep-retest: anchor b (epoch 1779364800) is outside the 306-bar window; its bar-index was estimated from bar_seconds=3600 (wall-clock spacing across any gap in the un-fetched span). Widen the candle fetch so anchors are in-window."
  ],
  "outcome": {
    "net_r": -1.0,
    "tp_hits": 0,
    "sl_hits": 1,
    "reversal_closes": 0,
    "expiry_closes": 0,
    "invalidation_closes": 0,
    "open_at_end": 0,
    "legs": [
      {
        "entry_time": "2026-08-06T15:00:00Z",
        "entry_price": 6123.9,
        "stop_loss": 6184.2,
        "take_profit": 5980.2,
        "exit_time": "2026-08-06T19:00:00Z",
        "exit_price": 6184.2,
        "exit_reason": "stopped_out",
        "r": -1.0
      }
    ]
  }
}
